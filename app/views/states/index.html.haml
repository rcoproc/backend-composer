%h3 Listagem dos Estados

= link_to 'Novo Estado', new_state_path, class: 'btn btn-primary'

%table.table.table-striped
  %thead
    %tr
      %th
      %th Nome
      %th UF
      %th País
      %th
      %th

  %tbody
    - @states.each do |state|
      %tr
        %td= link_to 'Alterar', edit_state_path(state), class: 'btn btn-info btn-sm'
        %td= state.name
        %td= state.acronym
        %td= state.country_name
        %td= link_to 'Visualizar', state, class: 'btn btn-default btn-sm'
        %td= link_to 'Excluir', state, :method => :delete, :data => { :confirm => 'Tem certeza?' }, class: 'btn btn-danger btn-sm'

= paginate @states

%br

