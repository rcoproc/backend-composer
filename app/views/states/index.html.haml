%h3 Listagem dos Estados

= link_to 'Novo Estado', new_state_path, class: 'btn btn-primary'

%p

  %table#states.table.table-bordered.table-striped{ cellspacing:"0" ,width: '100%' }
    %thead
      %tr
        %th
        %th Nome
        %th UF
        %th País
        %th
        %th

    %tbody
      - @states.each do |state|
        %tr 
          %td{ align: 'center' }= link_to 'Alterar', edit_state_path(state), class: 'btn btn-info btn-sm'
          %td
            = link_to state do 
              = simple_format(h(state.name))
          %td{ "data-href" => state_url(state) }
            = state.acronym
          %td{ "data-href" => state_url(state) }
            = state.country_name
          %td{ align: 'center '}= link_to 'Visualizar', state, class: 'btn btn-default btn-sm'
          %td{ align: 'center' }= link_to 'Excluir', state, :method => :delete, :data => { :confirm => 'Tem certeza?' }, class: 'btn btn-danger btn-sm'

%br

